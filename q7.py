
# a   O(n)
# b   O(n)
# c   O(n)
# d   O(n)
# e 
# f






# Takes in a SLL with n elements

#suppose that mystery_method() takes in a SinglyLinkedList as an argument
def mystery_method(l):
    # Assume that 'l' contains 'n' elements
    while not l.is_empty():
        l.remove_first()


# while the list is not empty, remove the first one 
# O(n)

#suppose that mystery_method() takes in a SinglyLinkedList as an argument
def mystery_method(l):
    # Assume that 'l' contains 'n' elements
    while not l.is_empty():
        l.remove_last()


# O(n)

#suppose that mystery_method() takes in a DoublyLinkedList as an argument
def mystery_method(l):
    # Assume that 'l' contains 'n' elements
    while not l.is_empty():
        l.remove_first()

# O(n
)



